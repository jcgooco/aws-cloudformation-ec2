name: "Configure AWS Region"
description: "Receives AWS Region and output it"
inputs:
  region:
    description: 'AWS Region to Deploy'
    required: true
    default: 'ap-southeast-1'
outputs:
  region: # id of output
    description: "AWS Region to deployed"
runs:
  using: "composite"
  steps:
    - run: aws cloudformation deploy --template-file ec2.yml --stack-name ec2-test-stack
      shell: bash

    